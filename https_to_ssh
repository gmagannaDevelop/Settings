Pour travailler avec la clé ssh et ne pas entrer le mot de passe
à chaque fois:

Le lien original (chez youtube):
  https://www.youtube.com/watch?v=6oTzYnQY17Q

Le lien officiel (chez github):
  https://help.github.com/articles/connecting-to-github-with-ssh/

La bonne façon de le démarrer:
  git remote set-url orgin git@github.com:gmagannaDevelop/repo_name.git

# Les commandes:
    cd ~/.ssh && ssh-keygen -t rsa -C "gmganna.biomed@gmail.com"

Après il faut aller sur github.com/settings/ssh et ajouter la clé publique.

# Pour ajouter un origin:
    git remote -v 
    git remote set-url origin git@github:gmagannaDevelop/repo.git

